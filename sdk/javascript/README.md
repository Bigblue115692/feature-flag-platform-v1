# JavaScript SDK

A small JavaScript client for remote evaluation.

The SDK demonstrates:

- constructor configuration
- async HTTP requests
- request timeout using AbortController
- retries
- in-memory TTL caching
- boolean convenience method
- value convenience method
